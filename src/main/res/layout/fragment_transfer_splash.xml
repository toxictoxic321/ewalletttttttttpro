<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:padding="32dp"
    tools:context=".TransferSplashFragment">

    <ImageView
        android:id="@+id/splash_check_icon"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginTop="100dp"
        android:src="@drawable/ic_check_placeholder"
        app:tint="#A5D6A7"
        android:contentDescription="Success Checkmark"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/splash_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Transfer Successful!"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/splash_check_icon" />

    <TextView
        android:id="@+id/splash_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="$-0.00"
        android:textColor="#A5D6A7"
        android:textSize="48sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/splash_title" />

    <!-- Details Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@drawable/input_background"
        app:layout_constraintTop_toBottomOf="@id/splash_amount">

        <TextView android:id="@+id/splash_recipient_id" style="@style/SplashDetailTextStyle" android:text="Recipient: User ID" />
        <TextView android:id="@+id/splash_source" style="@style/SplashDetailTextStyle" android:text="Source id : Your Name" />
        <TextView android:id="@+id/splash_time" style="@style/SplashDetailTextStyle" android:text="Time: Nov 15, 2025" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>